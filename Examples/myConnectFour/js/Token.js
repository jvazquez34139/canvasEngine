class Token{
	constructor(color, x, y){
		this.color = color;
		this.x = x;
		this.y = y;
		this.dropped = false;
	}
	//before dropped, make table display drop space
	//when dropped update player's tokens
	//when dropped update table
}