class Table{
	constructor(){
		//sets all spaces on the grid to available by default
		for(let y = 0; y < 6; i++){
			for(let x = 0; x < 7; i++){
				this.spaceAvailable[y][x] = true;
			}
		}
	}

	//get available spaces
	//update which spaces get are available
}